using System;
using System.Collections.Generic;
using System.Linq;

namespace LinqDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // List<string> countries = new List<string>()
            //{
            //    "India","US","Canada","England"
            //};

            // string cons = string.Empty;
            // //foreach(string country in countries)
            // //{
            // //    cons = cons + country + ",";
            // //}
            // int minl = 0;
            //  int lastIndex = cons.LastIndexOf(",");
            //// string cons1=cons.Remove(lastIndex);
            // string aggregatecountries = countries.Aggregate((a, b) => a + ","+b);
            //// int smallCountrylen = countries.Min(x => x.Length);
            // //Console.WriteLine(cons1);
            // Console.WriteLine($"small country length is {cons}");

            //RestrictionFuns.TestRes();
            //TestEmployee.TestEmp();
            //  TestStudents.TestStud();
            //   TestEmpoyeeOrderBy.TestOrderBy();
            //TestEmploye.TestEmp();
            //TestGroupJoin.TestJoin();
            //  TestInnerJoin.TestInner();
            //TestIndex.TestInd();
            //Sorting.SortEmps();
            //FirstConsoleApp.TestFirstApp();
           // TestSingleton.Test();
           FactoryPattern.FactoryDesignPattern();
        }
    }
}
